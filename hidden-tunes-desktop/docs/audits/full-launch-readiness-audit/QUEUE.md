# QUEUE Audit

Live queue owned by DesktopPlaybackProvider (`ApiSong[]`). Typed persist helpers in `lib/queue`. Smart continuation dirty. Queue panel in player shells; `QueueUpNextPanel` dead stub.

Auto-next: finite families via ended events + smart continuation WIP. Live radio/TV/sports must not fake-advance.

**Launch:** Partial — contracts PASS; dirty smartContinuation must be intentional before ship.
