# LIBRARY Audit

Typed library v2, playlists, history, downloads — contract-verified previously.  
Favorites sidebar = music Liked only (IA mismatch). Sports favorites unsupported. Local-only (no account sync).

**User data:** local device persistence, not account-synchronised.
