# Pagination

Country/category are passed on every page. Existing request IDs and abort controllers prevent stale pages appending after query/filter changes. Stable-ID dedupe remains in `useTvPageData`.
