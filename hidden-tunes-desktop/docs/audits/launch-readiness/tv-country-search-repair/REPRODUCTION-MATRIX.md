# Reproduction matrix

Before repair, `q=RO` returned 319 mixed-country rows while `country=RO` returned 59 Romanian rows. `q=GH` returned 33 mixed-country rows while `country=GH` returned 8 Ghanaian rows. Names entered the same dual-request merge. The defect reproduced in both TV-page and global-TV search through the shared service.
