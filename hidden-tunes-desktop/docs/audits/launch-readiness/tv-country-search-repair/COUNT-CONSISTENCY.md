# Count consistency

Region cards, direct country requests, and country Search now use the same endpoint, desktop platform, ISO country filter, and pagination parser. The prior `max(q total, country total)` calculation was removed.
