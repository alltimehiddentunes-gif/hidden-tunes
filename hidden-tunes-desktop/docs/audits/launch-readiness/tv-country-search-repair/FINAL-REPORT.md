# Final report

Country names and validated ISO codes now resolve through one registry and one request builder. Pure countries use only the canonical country filter; mixed/scoped searches preserve text, country, and category. Invalid payloads throw into the existing error/Retry state.
