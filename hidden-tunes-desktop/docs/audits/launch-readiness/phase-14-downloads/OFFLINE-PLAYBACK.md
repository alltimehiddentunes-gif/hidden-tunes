# Offline Playback

Completed items Play via `ht-download://` through existing audio owner.
Family pages call `applyLocalDownloadUrls` before `playQueue`.
Missing/corrupt are not playable; Downloads page offers Retry/Remove.
