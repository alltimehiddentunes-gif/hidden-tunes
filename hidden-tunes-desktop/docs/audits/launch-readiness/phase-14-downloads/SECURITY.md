# Security

Main owns path resolution; renderer never supplies destination paths.
HTTPS allowlist; live/HLS rejected; path jail via assertInsideRoot.
No service-role keys; signed URLs not persisted as permanent when resolve is used.
