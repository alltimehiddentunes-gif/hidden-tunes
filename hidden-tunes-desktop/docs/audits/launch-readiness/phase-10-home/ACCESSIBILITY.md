# Accessibility

- Section `h2` headings with `aria-labelledby`
- Play controls have accessible names
- Now playing via `aria-current` + visible badge/outline (not colour alone)
- Focus-visible rings on Home cards
- Reduced motion respected for mix enter/exit
- Artwork decorative (`alt=""` / `aria-hidden` shells)
- Album body vs Play are separate controls
