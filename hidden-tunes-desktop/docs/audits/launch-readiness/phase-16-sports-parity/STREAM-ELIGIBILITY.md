# Stream eligibility

Play requires the explicit streams flag and backend `isPlayable`, followed by a supported HTTPS resolution. Streams-off removes Play from cards/details and short-circuits dispatch. Fixture-only records cannot enter video ownership or fall back to TV.
