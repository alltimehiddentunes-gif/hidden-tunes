# Authority

Desktop package uses VITE_SUPABASE_URL + VITE_SUPABASE_ANON_KEY.
Same Supabase Auth users as mobile (kojcyswxfuikxmqntwye).
Backend validates Bearer JWTs for Follow — no desktop-only user table.
